version: "3.3"

services:

  rpi-spotify:
    build: .
    image: flaviostutz/rpi-spotify
    network_mode: host
    devices:
      - "/dev/snd:/dev/snd"
    environment:
      - SPOTIFY_NAME=MySpeaker
  
